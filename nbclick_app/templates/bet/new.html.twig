{% extends 'base.html.twig' %}

{% block title %}Nouveau pari{% endblock %}
{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            
            <div class="card shadow-lg rounded border-0">
                <div class="card-header text-white text-center" style="background-color: #C8102E;">
                    <h2>🏀 Parier sur le match NBA</h2>
                </div>
                
                <div class="card-body" style="background-color: #F5F5F5;">
                    <table class="table table-bordered text-center border-primary">
                        <thead style="background-color: #1D428A; color: white;">
                            <tr>
                                <th>Équipe à domicile</th>
                                <th>Équipe visiteuse</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr style="background-color: white;">
                                <td><strong>{{ game.home.name }}</strong></td>
                                <td><strong>{{ game.away.name }}</strong></td>
                            </tr>
                        </tbody>
                    </table>

                    <div class="alert text-center" style="background-color: #1D428A; color: white;">
                        <h4>💡 Votre sélection</h4>
                        <p><strong>Équipe choisie :</strong> {{ game[team]['name'] }}</p>
                        <p><strong>Cote :</strong> <span class="badge fs-5" style="background-color: #C8102E;">{{ cote }}</span></p>
                    </div>

                    {{ form_start(form) }}
                        <div class="mb-3">
                            {{ form_widget(form) }}
                        </div>
                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-lg text-white" style="background-color: #C8102E; border: none; transition: 0.3s;">
                                💰 Valider le pari
                            </button>
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>

        </div>
    </div>
</div>

<style>
    /* Effet de survol pour le bouton */
    button:hover {
        background-color: #A60D26 !important;
    }
</style>

{% endblock %}